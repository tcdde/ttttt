// OPTIONAL: Firebase setup example
// 1. Create a Firebase project.
// 2. Enable Authentication, Firestore and Hosting.
// 3. Replace the values below with your Firebase project settings.
// 4. Do not commit secret server credentials to public files.

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// import { initializeApp } from "https://www.gstatic.com/firebasejs/...";
// const app = initializeApp(firebaseConfig);
